{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "damage-reduction.schema.json",
  "title": "Damage reduction effect",
  "description": "Reduces the upcoming damage the user receives",
  "type": "object",
  "properties": {
    "amount": {
      "description": "The amount of damage subtracted from the incoming damage",
      "type": "integer",
      "minimum": 0
    }
  },
  "required": ["amount"],
  "additionalProperties": false
}