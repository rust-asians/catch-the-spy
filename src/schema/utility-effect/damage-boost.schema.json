{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "damage-boost.schema.json",
  "title": "Damage boost effect",
  "description": "Increases the damage of the user",
  "type": "object",
  "properties": {
    "amount": {
      "description": "The amount of damage added to the outgoing damage",
      "type": "integer",
      "minimum": 0
    }
  },
  "required": ["amount"],
  "additionalProperties": false
}